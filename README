
teams_cache/2/dist/のexeを実行すればGUIが立ち上がる。
ボタンを押せばキャッシュ削除開始。
teamsキャッシュのフォルダを探し、あれば削除する。


exe作成方法
pip install pyinstaller 
pyinstaller --onefile --noconsole --distpath . teams_cache_clear.py
#アイコン反映させるコマンド
pyinstaller --onefile --windowed --icon=teams_cache.ico teams_cache_clear.py


--distpath . → dist フォルダを作らず、カレントディレクトリに直接 exe を作成。

--workpath → ビルド時の一時ファイル

--specpath → .spec ファイルの出力先


exeバージョン履歴
-------------------------------------------------------------------------------------
2 teamsキャッシュクリアアプリを作成
3 teamsリンクが見つからない時、エラーメッセージを吐くように設定。これまではフリーズしていた
4 UIの変更。データモデルで削除状況を管理するようにした。データモデルは表形式でGUI表示する。GUIのパスをダブルクリックすると親フォルダを開く。名前をクリックすると削除をリトライする。

